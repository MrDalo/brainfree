import sys
from PyQt5.QtWidgets import QApplication, QStackedWidget
from application import LoginPage, Window
"""
app = QApplication(sys.argv)
login_poge = LoginPage()
window = Window()
multiple_screens = QStackedWidget()
multiple_screens.addWidget(login_poge)
multiple_screens.addWidget(window)
multiple_screens.setFixedSize(600, 600)
multiple_screens.show()

sys.exit(app.exec_())"""